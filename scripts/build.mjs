import {contactPage} from '../content/contact.mjs';
import {aboutPage} from '../content/about.mjs';
import {mkdir,writeFile,cp,rm,readFile} from 'node:fs/promises';
import {createHash} from 'node:crypto';
import {cities,services,slug,projects} from '../content/site.mjs';
import {renderServices} from './service-pages.mjs';
import {production,metadata,sitemap,siteOrigin} from './seo.mjs';
import {projectPhotoCaptions} from '../content/photo-captions.mjs';
import {commercialGallery} from '../content/gallery.mjs';
import {localGuides} from '../content/local-services.mjs';
const out=new URL('../dist/',import.meta.url);await rm(out,{recursive:true,force:true});await mkdir(out,{recursive:true});await cp(new URL('../public/',import.meta.url),out,{recursive:true});
const dimensions=JSON.parse(await readFile(new URL('../content/image-dimensions.json',import.meta.url)));
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('"','&quot;');
const photo=(id,alt,hero=false,fullWidth=false)=>{const large=id===16?'720':'1600';const file=`project-${id}-${large}.webp`;const [width,height]=dimensions[file];const candidates=id===16?`/assets/${file} ${width}w`:`/assets/project-${id}-720.webp ${dimensions[`project-${id}-720.webp`][0]}w, /assets/${file} ${width}w`;return `<img data-photo-id="${id}" src="/assets/${file}" srcset="${candidates}" sizes="${fullWidth?'(min-width:1423px) 1280px, 90vw':hero?'(max-width:800px) 90vw, (min-width:1423px) 602px, 43vw':'(max-width:600px) 90vw, (min-width:1423px) 626px, 44vw'}" width="${width}" height="${height}" ${hero?'fetchpriority="high" loading="eager"':'loading="lazy"'} decoding="async" alt="${esc(alt)}">`;};
const galleryPhoto=(id,alt)=>`<a class="gallery-photo" href="/assets/project-${id}-1600.webp" target="_blank" rel="noopener" aria-label="${esc(`View full photo: ${alt} (opens in a new tab)`)}">${photo(id,alt)}<span class="photo-expand" aria-hidden="true">View full photo ↗</span></a>`;
const button=(href,text)=>`<a class="button" href="${href}">${text} ↗</a>`;
const header=`<a class="skip" href="#main">Skip to content</a>${production?'':'<div class="preview-note">Design preview · Website in development</div>'}<header class="wrap header"><a class="brand" href="/" aria-label="Verde Landscape Services home"><img src="/assets/verde-logo.webp" width="600" height="284" alt="Verde Landscape Services"></a><nav aria-label="Main navigation"><a href="/case-studies/">Our work</a><a href="/gallery/">Gallery</a><a href="/services/">Services</a><a href="/about/">About</a><a class="mobile-contact" href="/contact/">Contact</a>${button('/contact/','Discuss your project')}</nav></header>`;
const closing=`<section class="closing green"><div class="wrap"><div><p class="eyebrow">Start a conversation</p><h2>Let’s build something lasting.</h2><p>Tell us about your property and what you have in mind.</p></div><a class="button light" href="/contact/">Discuss your project ↗</a></div></section>`;
const footer=`<footer class="footer"><div class="wrap"><p>Verde Landscape Services<br>Central Texas, through and through.</p><p><a href="tel:+17373009848">737-300-9848</a><br><a href="mailto:hello&#64;verdelandscapes&#46;com">hello&#64;verdelandscapes&#46;com</a></p><p><a href="/gallery/">Gallery</a> · <a href="/services/">Services</a><br>© ${new Date().getFullYear()} Verde Landscape Services</p></div></footer>`;
const cards=(ps,heading=3)=>`<div class="grid">${ps.map(p=>`<a class="card" href="/case-studies/${p.slug}/">${photo(p.image,p.alt)}<div class="label"><h${heading}>${p.name}</h${heading}><span aria-hidden="true">↗</span></div><p>${p.cardSubtitle || `${p.location} · ${p.type}`}</p></a>`).join('')}</div>`;
const pages=[];
async function page(path,title,description,body){const dir=new URL(path.slice(1),out);await mkdir(dir,{recursive:true});const currentSection=path.startsWith('/case-studies/')?'/case-studies/':path.startsWith('/services/')||cities.some(c=>path.startsWith(`/${slug(c)}/`))?'/services/':path;
const pageHeader=header.replaceAll(`href="${currentSection}"`,`href="${currentSection}" aria-current="${currentSection===path?'page':'location'}"`);
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">${metadata(path,title,description)}<title>${esc(title)} | Verde Landscape Services</title><meta name="description" content="${esc(description)}"><meta name="theme-color" content="#133D25"><link rel="stylesheet" href="/styles.css"></head><body>${pageHeader}<main id="main">${body}</main>${path==='/contact/'?'':closing}${footer}</body></html>`;await writeFile(new URL('index.html',dir),html);pages.push({path,title});}
const areas=`<div class="area-directory">${localGuides.map(g=>`<details><summary>${esc(g.city)}, TX</summary><ul class="service-list">${services.map(s=>`<li><a href="/${slug(g.city)}/${slug(s)}/">${esc(s)} in ${esc(g.city)}</a></li>`).join('')}</ul></details>`).join('')}</div>`;
await page('/','Central Texas Landscape & Hardscape Construction Experts','Residential and commercial landscape and hardscape construction across Central Texas. Explore Verde’s work and discuss your next project.',`<div class="wrap"><section class="hero"><div><p class="eyebrow">Residential & commercial • Central Texas</p><h1>Your Central Texas landscape & hardscape construction experts.</h1><p>Specializing in residential and commercial projects, with decades of experience building beautiful landscapes that last.</p><div class="actions">${button('/contact/','Discuss your project')}<a href="/case-studies/">Explore our work ↗</a></div></div><figure>${photo(9,'Limestone boulders and planted beds beside a shaded walkway at Providence Estates Townhomes',true)}<figcaption>Selected work / Providence Estates Townhomes</figcaption></figure></section><div class="proof"><span>Residential & commercial</span><span>Decades of experience</span><span>Central Texas expertise</span></div><section class="section"><div class="section-top"><div><p class="eyebrow">Selected work</p><h2>Built with care.<br>Made to last.</h2></div><a href="/case-studies/">View all projects ↗</a></div>${cards(projects.slice(0,2))}</section></div><section class="section green"><div class="wrap"><p class="eyebrow">What we do</p><h2>From the ground up.</h2><div class="service-grid">${[['Tree Services','Thoughtful tree care, trimming, and clearing to support the next chapter of your property.'],['Landscape Construction','Planting, irrigation, sod, and complete landscape transformations for homes and commercial properties.'],['Outdoor Hardscapes','Stonework, paths, fencing, and outdoor structures that give your landscape form and purpose.'],['Drainage & Erosion Control','Site improvements that address the movement of water and support a more resilient landscape.']].map(([t,d],i)=>`<article class="service"><span class="number">0${i+1}</span><h3>${t}</h3><p>${d}</p><a href="/services/#category-${i}">Explore services ↗</a></article>`).join('')}</div></div></section><section class="section wrap split"><div><p class="eyebrow">Local knowledge. Lasting relationships.</p><h2>Central Texas<br>through and through.</h2><p>We know that a beautiful landscape starts with understanding the property. Site conditions, water, materials, and the way a space will be used all shape how we approach the work.</p><a href="/about/">Get to know Verde ↗</a></div><div><h3>Spaces that feel like home.</h3><p>From new construction to established landscapes ready for a refresh, we bring residential projects together with care and attention to the whole property.</p><div class="rule"></div><h3>Trusted commercial partnerships.</h3><p>For property owners, managers, and project teams, we deliver landscape and hardscape construction with clear scopes and coordinated execution.</p></div></section>`);
await page('/case-studies/','Our Work','Explore Verde’s residential, ranch, and commercial landscape construction projects.',`<section class="wrap section"><p class="eyebrow">Our work</p><h1>Different properties.<br>The same care.</h1><p>Explore the planning, materials, and craftsmanship behind our projects.</p>${cards(projects,2)}<div class="contact-panel"><h2>More commercial work.</h2><p>Explore landscape refreshes and outdoor amenities across our broader commercial project collection.</p>${button('/gallery/','Explore the commercial gallery')}</div></section>`);
for(const p of projects)await page(`/case-studies/${p.slug}/`,p.name,p.summary,`<div class="wrap"><section class="page-title"><p class="breadcrumb"><a href="/case-studies/">Our work</a> / ${p.name}</p><p class="eyebrow">${p.location} · ${p.type}</p><h1>${p.name}</h1><h2>${p.intro}</h2><p>${p.summary}</p></section><div class="banner">${photo(p.image,p.alt,true,true)}</div><div class="facts">${p.facts.map(f=>`<span>${f}</span>`).join('')}</div><section class="story">${p.sections.map(([h,t])=>`<article><h2>${h}</h2><p>${t}</p></article>`).join('')}<h2 class="gallery-heading">Project gallery</h2><p class="gallery-note">Select a photo to see the full image.</p><div class="gallery">${p.gallery.filter(i=>i!==p.image).map(i=>`<figure>${galleryPhoto(i,projectPhotoCaptions[i] || `Landscape work at ${p.name}`)}<figcaption>${esc(projectPhotoCaptions[i] || p.name)}</figcaption></figure>`).join('')}</div></section></div>`);
await page('/gallery/','Commercial Project Gallery','Explore Verde’s commercial landscape projects, including apartment landscape improvements, shared gathering spaces, and outdoor amenities.',`<section class="wrap section"><p class="eyebrow">Commercial project gallery</p><h1>Places to gather.<br>Room to thrive.</h1><p>A curated look at landscape improvements and shared outdoor spaces across our commercial projects.</p><p class="gallery-note">Select a photo to see the full image.</p><div class="gallery commercial-gallery">${commercialGallery.map(p=>`<figure>${galleryPhoto(p.image,p.caption)}<figcaption><h2>${p.name}</h2><p>${p.caption}</p></figcaption></figure>`).join('')}</div><div class="contact-panel"><h2>See the complete project stories.</h2><p>Our case studies take a closer look at the scope and construction behind selected residential and commercial properties.</p>${button('/case-studies/','Explore our work')}</div></section>`);
await page('/services/','Landscape & Hardscape Services','Explore tree services, landscape construction, outdoor hardscapes, drainage and erosion control from Verde.',`<section class="wrap section"><p class="eyebrow">Our capabilities</p><h1>One partner.<br>A complete landscape.</h1><p>Residential and commercial construction, from preparation and infrastructure to planting and the finished outdoor space.</p><div class="grid">${[['Tree Services',['Tree Trimming','Tree Services','Land Clearing']],['Landscape Construction',['Landscaping','Lawn Care','Sprinkler System Repair','Artificial Turf','Sod Installation','Sprinkler System Installation']],['Outdoor Hardscapes',['Fencing','Composite Decking','Masonry']],['Drainage & Erosion Control',['French Drain']]].map(([t,ss],i)=>`<article id="category-${i}" class="contact-panel"><h2>${t}</h2><ul>${ss.map(s=>`<li><a href="/services/${slug(s)}/">${s}</a></li>`).join('')}</ul></article>`).join('')}</div><h2>Across Central Texas.</h2>${areas}<div class="actions">${button('/contact/','Discuss your scope')}<a href="/austin/landscaping/">Landscaping in Austin ↗</a></div></section>`);
await page('/about/','About Verde','Austin and Central Texas landscape construction expertise, with more than 30 years of crew experience serving residential and commercial properties.',aboutPage(photo));
await page('/contact/','Discuss Your Project','Contact Verde Landscape Services to discuss a residential or commercial landscape construction project.',contactPage);
await renderServices(page,esc,button);
if(production && pages.length!==179) throw new Error('Complete the 179-page scope before production indexing.');
await writeFile(new URL('robots.txt',out),production?`User-agent: *\nAllow: /\nSitemap: ${siteOrigin}/sitemap.xml\n`:'User-agent: *\nDisallow: /\n');
if(production) await writeFile(new URL('sitemap.xml',out),sitemap(pages));
await writeFile(new URL('build-config.json',out),JSON.stringify({production}));
await writeFile(new URL('404.html',out),'<!doctype html><html lang="en"><meta charset="utf-8"><meta name="robots" content="noindex"><title>Page not found | Verde</title><link rel="stylesheet" href="/styles.css"><main class="wrap not-found"><h1>That page isn’t here.</h1><a href="/">Return to Verde</a></main></html>');
await writeFile(new URL('routes.json',out),JSON.stringify(pages,null,2));
const styles=await readFile(new URL('../public/styles.css',import.meta.url),'utf8');
const contentFingerprint=createHash('sha256').update(JSON.stringify({localGuides,projects,commercialGallery,contactPage,about:aboutPage(photo),styles})).digest('hex').slice(0,16);
await writeFile(new URL('build-info.json',out),JSON.stringify({release:'2026-09-21-gallery-editorial-review',pageCount:pages.length,contentFingerprint,mode:production?'production':'preview'},null,2));
const plan=[...['/','/about/','/contact/','/gallery/','/case-studies/','/services/',...projects.map(p=>`/case-studies/${p.slug}/`)],...services.map(s=>`/services/${slug(s)}/`),...cities.flatMap(c=>services.map(s=>`/${slug(c)}/${slug(s)}/`))];
await mkdir(new URL('../planning/',import.meta.url),{recursive:true});await writeFile(new URL('../planning/page-plan.json',import.meta.url),JSON.stringify(plan.map(path=>({path,status:pages.some(p=>p.path===path)?'review-draft':'content-required'})),null,2));
console.log(`Built ${pages.length} review pages; ${plan.length} planned pages. Indexing ${production?'enabled':'disabled'}.`);
